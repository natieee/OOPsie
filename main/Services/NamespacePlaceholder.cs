namespace main.Services
{
    // Keeps the main.Services namespace available to the compiler.
    // Remove this file once your real service types (e.g. AuthService) are in namespace main.Services.
    internal static class NamespacePlaceholder
    {
    }
}